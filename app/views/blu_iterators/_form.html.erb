<%= form_with(model: blu_iterator) do |form| %>
  <% if blu_iterator.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(blu_iterator.errors.count, "error") %> prohibited this blu_iterator from being saved:</h2>

      <ul>
        <% blu_iterator.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
