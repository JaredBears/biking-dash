<%= form_with(model: reported_car) do |form| %>
  <% if reported_car.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reported_car.errors.count, "error") %> prohibited this reported_car from being saved:</h2>

      <ul>
        <% reported_car.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :car_id, style: "display: block" %>
    <%= form.text_field :car_id %>
  </div>

  <div>
    <%= form.label :report_id, style: "display: block" %>
    <%= form.text_field :report_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
