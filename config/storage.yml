test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>
local:
  service: Disk
  root: <%= Rails.root.join("storage") %>
cloudinary_dev:
  service: Cloudinary
  folder: images_dev
cloudinary_prod:
  service: Cloudinary
  folder: images
